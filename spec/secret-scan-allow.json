{
  "_comment": "Maps the sha256 of a reviewed value to why it is safe, so the allowlist never contains a secret itself. Compute a hash with: node -e \"console.log(require('crypto').createHash('sha256').update(process.argv[1]).digest('hex'))\" -- '<value>'"
}
